import headerStyles from '../styles/Header.module.css'

const Header = () => {
    return (
        <><h1 className={headerStyles.title}>People and Pets</h1>
        <h2>Click on the name of a Person to view the profile</h2></>
        
    )
}

export default Header